# Ejercicio 9: Verificar si una lista está vacía

def is_empty(lista): # Verifica si la lista está vacía; devuelve True si no tiene elementos, False en caso contrario
    if len(lista) == 0:
        return True
    else:
        return False